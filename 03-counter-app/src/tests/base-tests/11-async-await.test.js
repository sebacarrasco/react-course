import { getImagen } from "../../base-tests/11-async-await"

describe('Pruebas con async y await', () => {
    
    test('should return url of an image', async() => {
        
        // const url = await getImagen();
        // expect(typeof url).toBe("string");
        // Lo dejo comentado porque hay que cambiar la api_key
    });
    
});
